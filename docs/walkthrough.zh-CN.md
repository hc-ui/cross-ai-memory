# 在这本笔记里过一周

和 `aimem tour` 是同一件事。下面每个链接都是仓库里的真文件。

林可是虚构的。一周是方法。

## 周一 · Cursor 写下一条事实

林可：新软件放到 `E:/Apps`。

Cursor 先不写库，只提案 `CAND-20260824-01`。
林可说「同意写入」。这才出现：

[examples/lin-ke/shared/install-path.md](../examples/lin-ke/shared/install-path.md)

一个主题，一篇笔记，变更日志一行：`MEM-20260824-01`。

采集器眼里的周一对话：

```bash
aimem collect normalize --source cursor --path examples/sessions/cursor-monday.jsonl
```

## 周三 · Claude 读同一篇

新窗口，另一家产品。Claude 从 [MEMORY.md](../examples/lin-ke/MEMORY.md) 进，再读索引，再读安装路径。

它没有再问文件该放哪。

一本共用笔记要的就是这个。

## 周日 · Codex 只出清单

Codex 扫过本周本地会话，只写提案。正文一篇没动。

打开 [proposals/week-2026-08-24.md](../examples/lin-ke/proposals/week-2026-08-24.md)。

| ID | 内容 | 结果 |
| --- | --- | --- |
| CAND-20260824-01 | 安装路径 | 批准并写入 |
| CAND-20260824-02 | 靠改 README 涨星 | 驳回，以后别再列 |
| CAND-20260824-03 | leafbox 手册语气 | 还开着 |

## 被改回去的那条

[work/leafbox.md](../examples/lin-ke/work/leafbox.md) 先写过「Windows 用 GBK」。两天后林可改了。被否的方案留在篇里，免得下场对话再捡回来。

写在笔记里的错，改得掉。
写在厂商脑子里的错，通常改不掉。
